# Copy to .env and fill in values (do not commit real secrets).

# Outbound-only Twilio number used for trial drip SMS (BOO-99A)
# Fallback: BOOK8_SYSTEM_SMS_FROM
TWILIO_TRIAL_NOTIFY_FROM=
BOOK8_SYSTEM_SMS_FROM=
